create database School;


create table VENDOR (
	V_CODE int primary key,
    V_NAME varchar(50),
    V_CONTACT varchar(50),
    V_AREACODE varchar(10),
    V_PHONE varchar(15),
    V_STATE varchar(20),
    V_ORDER varchar(50)
);
